# Setup Instructions for MCG-LINEA Next.js Project

## ✅ Что уже сделано

1. ✅ Структура Next.js проекта с App Router
2. ✅ TypeScript конфигурация
3. ✅ Tailwind CSS + дизайн-система
4. ✅ Базовые UI компоненты (Button, Input, Card)
5. ✅ Layout компоненты (Header, Footer, CookieBanner)
6. ✅ Главная страница с секциями (Hero, Features, Brands, How It Works, CTA)
7. ✅ Базовые страницы (Catalog, Cart, About, Contacts, Payments)
8. ✅ TypeScript типы для всех сущностей

## 🚀 Следующие шаги для продолжения разработки

### 1. Установка зависимостей

```bash
npm install
# или
yarn install
# или
pnpm install
```

### 2. Запуск проекта

```bash
npm run dev
```

Откройте http://localhost:3000

### 3. Настройка данных (Варианты)

#### Вариант A: Подключение к существующему API
- Создайте файл `.env.local` с переменными API
- Настройте API routes в `src/app/api/`
- Создайте клиент для работы с API в `src/lib/api.ts`

#### Вариант B: Создание локальной БД (Prisma + PostgreSQL)
- Установите Prisma: `npm install prisma @prisma/client`
- Инициализируйте: `npx prisma init`
- Настройте схему в `prisma/schema.prisma`
- Создайте миграции: `npx prisma migrate dev`

### 4. Что нужно доделать

#### Приоритет 1 (MVP):
- [ ] Настроить данные (API или Prisma)
- [ ] Реализовать поиск с autocomplete
- [ ] Создать страницу товара (`/product/[slug]`)
- [ ] Реализовать функционал корзины (state management)
- [ ] Создать checkout flow
- [ ] Реализовать аутентификацию
- [ ] Личный кабинет (профиль, заказы)

#### Приоритет 2:
- [ ] Фильтры и сортировка в каталоге
- [ ] Генерация PDF инвойсов
- [ ] Email уведомления
- [ ] SEO оптимизация (sitemap, robots.txt, schema.org)
- [ ] Интеграция платежей

#### Приоритет 3:
- [ ] B2B pricing tiers
- [ ] Мультиязычность (i18n)
- [ ] VIN lookup
- [ ] Advanced поиск

### 5. Структура компонентов

Все компоненты организованы в папках:
- `src/components/ui/` - базовые UI компоненты (shadcn/ui)
- `src/components/layout/` - Header, Footer, CookieBanner
- `src/components/home/` - секции главной страницы
- `src/components/catalog/` - компоненты каталога
- `src/components/cart/` - компоненты корзины

### 6. Стили и дизайн

Дизайн-система настроена через Tailwind CSS с кастомными токенами в `tailwind.config.ts`.
Цвета, шрифты и радиусы определены в `src/styles/globals.css`.

### 7. Изображения

Изображения должны находиться в `public/assets/`. 
Для использования в компонентах используйте:
- `next/image` для оптимизации изображений
- Путь: `/assets/filename.jpg`

### 8. Дополнительные UI компоненты

При необходимости добавьте больше компонентов из shadcn/ui:
```bash
npx shadcn@latest add [component-name]
```

Например:
- `textarea` - для форм
- `select` - для выпадающих списков
- `dialog` - для модальных окон
- `toast` - для уведомлений
- `skeleton` - для загрузки

## 📝 Примечания

- Проект использует Next.js 15 с App Router
- Все компоненты написаны на TypeScript
- Стилизация через Tailwind CSS
- UI компоненты на базе Radix UI (через shadcn/ui)
- Формы: React Hook Form + Zod для валидации

## 🔗 Полезные ссылки

- [Next.js Documentation](https://nextjs.org/docs)
- [Tailwind CSS](https://tailwindcss.com/docs)
- [shadcn/ui](https://ui.shadcn.com)
- [Radix UI](https://www.radix-ui.com)

